# 答辩问答

每个组员都应该读一遍。回答时不要死背，可以用自己的话说。

## 基础问题

### Q1. 你们项目是什么？

我们的项目是校园实验室设备预约与维护系统，用来管理实验室共享设备的查询、预约、审批、维修、库存和报表。

### Q2. 为什么它是 information system？

因为它存储和处理结构化信息，包括用户、实验室、设备、课程、预约、审批、维修、库存和报表。用户可以通过 Web 页面对这些信息进行查询、新增、更新和分析。

### Q3. 为什么适合数据库课程？

因为它不只是简单 CRUD。它包含多表关系、多对多关系、主键、外键、检查约束、唯一约束、索引、视图、事务和聚合查询。

### Q4. 前端是什么？

前端是 HTML、CSS、JavaScript 写的 Web 页面。Java 程序启动一个轻量 HTTP server，浏览器通过 API 和后端通信。

## 数据库问题

### Q5. 主要有哪些表？

主要有：

`users`、`labs`、`equipment`、`courses`、`course_members`、`equipment_course_access`、`reservations`、`approvals`、`maintenance_tickets`、`maintenance_updates`、`consumables`、`stock_transactions`。

### Q6. 举一个多对多关系。

用户和课程是多对多，一个用户可以参加多个课程，一个课程也有多个用户，所以用 `course_members` 作为中间表。

另一个例子是设备和课程：一个设备可以被多个课程使用，一个课程也可以使用多个设备，所以用 `equipment_course_access`。

### Q7. 为什么要有 `approvals` 表？

`reservations` 表保存预约当前状态，比如 PENDING 或 APPROVED。`approvals` 表保存审批历史，包括谁审批、什么时候审批、结果是什么、备注是什么。

### Q8. 为什么要有 `stock_transactions`？

`consumables` 只保存当前库存数量。`stock_transactions` 保存每一次库存变化，包括谁改的、改了多少、原因是什么、什么时候改的。这样有审计记录。

### Q9. 用了哪些约束？

用了主键、外键、唯一约束、检查约束和预约时间检查。例如库存数量不能小于 0，预约结束时间必须晚于开始时间。

## 事务问题

### Q10. 如何判断预约时间冲突？

判断条件是：

```text
existing.start_time < new_end AND existing.end_time > new_start
```

也就是说，已有预约的开始时间早于新预约结束时间，并且已有预约的结束时间晚于新预约开始时间，就说明有重叠。

### Q11. 为什么预约要用事务？

因为检查设备状态、检查时间冲突、插入预约记录应该作为一个整体完成。如果中间失败，就应该 rollback，否则数据库可能不一致。

### Q12. 事务例子有哪些？

库存更新就是一个例子：

1. 锁定耗材行；
2. 读取当前库存；
3. 计算新库存；
4. 如果新库存小于 0 就拒绝；
5. 更新库存；
6. 插入库存流水；
7. commit。

## 实现问题

### Q13. 为什么用 JDBC？

作业说不推荐 ORM 和复杂 MVC 框架。JDBC 可以直接展示 SQL 和数据库操作，更适合数据库课程。

### Q14. 为什么用 MySQL？

因为课程 lab 主要学的是 MySQL，用 MySQL 更符合课程内容，也更容易在答辩时解释。项目仍然使用 JDBC 直接操作数据库，启动时会自动建表并插入 seed 数据。

### Q15. 为什么改成 Web 前端？

因为我们学过 Web，HTML/CSS/JavaScript 更容易解释。作业 PDF 里也明确举了 Web 作为 UI 例子。

### Q16. 前端怎么和后端连接？

`app.js` 用 `fetch()` 调 Java 后端 API，比如 `/api/equipment`、`/api/reservations/create`。Java API 再调用 service，service 调 DAO，DAO 用 JDBC 操作 MySQL 数据库。

### Q17. 自动化测试测了什么？

测试了：

- 正确登录；
- 错误密码登录失败；
- 预约冲突会被拒绝；
- 库存不能变负；
- 正常库存更新会改变数量；
- 报修后设备状态变成 MAINTENANCE。

## 亮点和不足

### Q18. 这个项目最强的数据库点是什么？

它不只是增删改查。它有预约冲突检查、多对多关系、审批历史、维修流程、库存流水、SQL 视图和事务。

### Q19. 最大限制是什么？

密码现在是明文存储，只适合课程演示。真实系统应该使用密码哈希和更严格的权限控制。

### Q20. 未来可以怎么改进？

可以加入密码哈希、邮件通知、二维码签到、设备图片上传、更细的权限表和更完整的审计日志。

## 每个人的一句话定位

### Member A

我负责项目集成和预约模块，可以解释预约如何创建、如何判断时间冲突、如何审批。

### Member B

我负责数据库设计，可以解释 ER 图、关系模型、多对多关系、约束、视图和索引。

### Member C

我负责设备和维修，可以解释报修后如何创建工单，以及设备状态如何变化。

### Member D

我负责库存和报表，可以解释库存流水、防止负库存和报表查询。

### Member E

我负责 Web 前端、测试和文档，可以解释浏览器如何调用 Java API，以及测试覆盖了什么。
